using UnityEngine;
using UnityEngine.Events;
using UnityEngine.SceneManagement;

using Common;

namespace WarningsDisabler
{
	
	//		SceneManager.sceneLoaded += new UnityAction<Scene, LoadSceneMode>(Main.OnSceneLoaded);
	//	}

	//	private static void OnSceneLoaded(Scene scene, LoadSceneMode mode)
	//	{
	//		$"On scene loaded: {scene.name}".onScreen().logDbg();

	//	}
	//}
}